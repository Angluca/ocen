/// format-range: 4:6

import std::vector::{ Vector }

def foo(x: i32, y: i32): i32 {
    return x + y
}

def  bar(  a : str  ) {
    println(  "hello %s" ,    a  )
}

def   baz(  ) : bool {
    return    true
}
