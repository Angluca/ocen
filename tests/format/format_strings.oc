/// format

def foo(a: u32, b: u32, c: u32, d: u32): u32 => a + b + c + d // This is a comment

def main() {
    let var = f"val is {1+2}"// inline comment
    println(f"val is {foo(1,2,3,4)+5}") // another comment
}